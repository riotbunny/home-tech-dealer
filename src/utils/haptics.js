export const triggerHaptic = (type = 'light') => { if (typeof navigator === 'undefined' || !navigator.vibrate) return; try { switch (type) { case 'light': navigator.vibrate(30); break; case 'medium': navigator.vibrate(50); break; case 'heavy': navigator.vibrate(100); break; case 'success': navigator.vibrate([30, 60, 30]); break; case 'error': navigator.vibrate([40, 40, 40, 40, 40]); break; default: navigator.vibrate(30); } } catch (e) {} };
